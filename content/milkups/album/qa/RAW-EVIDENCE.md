# RAW-EVIDENCE.md — Bounded QA Evidence Sweep: MilkUps album
Generated by: ClickClack (Developer)
Repo root at time of run: /home/zax/Biz/z-dot-team
Run scope: steps 1, 3(artifact-path check), 4, 5, 7, 8 of the bounded brief. Steps 2 and 6 were SKIPPED per brief rule ("If the directory is absent, print MISSING: content/milkups/album/ and SKIP to step 7") because the album directory does not exist on disk.

NOTE (provenance, not interpretation): content/milkups/album/qa/ was CREATED BY THIS RUN solely to hold this file, as explicitly instructed by step 8. It is not one of the 9 expected album artifacts.

--------------------------------------------------------------------
## STEP 1a — `pwd && ls -la content/milkups/album/`
--------------------------------------------------------------------
$ pwd && ls -la content/milkups/album/ 2>&1; echo "LS_EXIT=$?"
/home/zax/Biz/z-dot-team
ls: cannot access 'content/milkups/album/': No such file or directory
LS_EXIT=2

RESULT: MISSING: content/milkups/album/

--------------------------------------------------------------------
## STEP 1b — `find content/milkups/album -type f -exec ls -l {} +`
--------------------------------------------------------------------
$ find content/milkups/album -type f -exec ls -l {} + 2>&1; echo "FIND_EXIT=$?"
find: 'content/milkups/album': No such file or directory
FIND_EXIT=1

--------------------------------------------------------------------
## STEP 1c — fallback `find content/milkups/album -type f | xargs -r ls -l`
--------------------------------------------------------------------
$ find content/milkups/album -type f | xargs -r ls -l 2>&1; echo "FALLBACK_EXIT=$?"
FALLBACK_EXIT=0
STDERR:
find: 'content/milkups/album': No such file or directory

(no file rows emitted — empty file set)

--------------------------------------------------------------------
## STEP 2 — per-file path + byte size + sha256sum
--------------------------------------------------------------------
SKIPPED — step 1 found zero files (directory absent). No sha256sum performable.

--------------------------------------------------------------------
## STEP 3 — EXISTS / MISSING for the 9 expected artifacts
--------------------------------------------------------------------
$ for p in content/milkups/album/album-manifest.json content/milkups/album/modules content/milkups/album/projects content/milkups/album/renders content/milkups/album/README.md content/milkups/album/TRACKLIST.md content/milkups/album/gtm/RELEASE-PLAN.md content/milkups/album/sales/MONETIZATION.md content/milkups/album/finance/BUDGET.md; do if [ -e "$p" ]; then echo "$p -> EXISTS"; else echo "$p -> MISSING"; fi; done

content/milkups/album/album-manifest.json -> MISSING
content/milkups/album/modules -> MISSING
content/milkups/album/projects -> MISSING
content/milkups/album/renders -> MISSING
content/milkups/album/README.md -> MISSING
content/milkups/album/TRACKLIST.md -> MISSING
content/milkups/album/gtm/RELEASE-PLAN.md -> MISSING
content/milkups/album/sales/MONETIZATION.md -> MISSING
content/milkups/album/finance/BUDGET.md -> MISSING

--------------------------------------------------------------------
## STEP 4 — manifest JSON test
--------------------------------------------------------------------
$ python3 -m json.tool content/milkups/album/album-manifest.json > /dev/null; echo EXIT=$?
usage: python -m json.tool [-h] [--sort-keys] [--no-ensure-ascii]
                           [--json-lines]
                           [--indent INDENT | --tab | --no-indent | --compact]
                           [infile] [outfile]
python -m json.tool: error: argument infile: can't open 'content/milkups/album/album-manifest.json': [Errno 2] No such file or directory: 'content/milkups/album/album-manifest.json'
EXIT=2

RESULT: BLOCKED — EXIT=2 (expected EXIT=0). Manifest file does not exist. No retry warranted: same filesystem state, and the brief allows one retry only for a transient failure. Attempts used: 1 of 2.

--------------------------------------------------------------------
## STEP 5 — .xm module load test
--------------------------------------------------------------------
$ for f in content/milkups/album/modules/*.xm; do xmp --load-only "$f"; echo "$f -> EXIT=$?"; done
Extended Module Player 4.1.0
Copyright (C) 1996-2016 Claudio Matsuoka and Hipolito Carraro Jr
Using null output
Mixer set to 44100 Hz, 16bit, cubic spline interpolated stereo
Press 'h' for help

Loading content/milkups/album/modules/*.xm (1 of 1)
xmp: content/milkups/album/modules/*.xm: No such file or directory
content/milkups/album/modules/*.xm -> EXIT=0

$ which xmp
/usr/bin/xmp
WHICH_EXIT=0

RESULT: BLOCKED — xmp IS installed (/usr/bin/xmp, Extended Module Player 4.1.0). Zero .xm files exist, so nothing was loaded. IMPORTANT: the literal "-> EXIT=0" above is a FALSE PASS artifact — the shell did not expand the glob (no nodescend/nullglob) and xmp was handed the literal string 'content/milkups/album/modules/*.xm' (note the `Loading ... (1 of 1)` line followed by `No such file or directory`). Do not read that EXIT=0 as "module loads". A real module load test requires actual files and a nullglob guard. Attempts used: 1 of 2.

--------------------------------------------------------------------
## STEP 6 — .mmp project XML well-formedness
--------------------------------------------------------------------
SKIPPED per brief (album directory absent -> skip to step 7). Zero .mmp files exist; no first-8-bytes inspection possible.

--------------------------------------------------------------------
## STEP 7 — renders ffprobe
--------------------------------------------------------------------
$ for f in content/milkups/album/renders/*; do echo "== $f"; ffprobe -v error -show_entries format=duration,size,bit_rate,format_name -of default=nw=1 "$f"; echo "ffprobe EXIT=$?"; done
== content/milkups/album/renders/*
content/milkups/album/renders/*: No such file or directory
ffprobe EXIT=1

RESULT: BLOCKED — ffprobe EXIT=1, renders/ directory absent. No rendered audio on disk to probe. Attempts used: 1 of 2.

--------------------------------------------------------------------
## STEP 8 — additional bounded evidence
--------------------------------------------------------------------
(a) Parent directory listing (single bounded `ls -l content/milkups/`, run to confirm the album subtree genuinely is not present under a different name):
$ ls -l content/milkups/ 2>&1; echo "EXIT=$?"
total 13392
-rw-r--r-- 1 zax zax    18403 Sep  7 01:56 album.html
drwxr-xr-x 3 zax zax     4096 Sep 12 01:12 art
drwxr-xr-x 2 zax zax     4096 Sep  4 23:18 assets
-rw-r--r-- 1 zax zax     9972 Sep  6 04:44 index.html
-rw-r--r-- 1 zax zax 13671463 Sep  6 03:32 TheShelvesRaisedUs-milkups-464737.zip
EXIT=0

(b) This file's own write (step 8 output target):
$ mkdir -p content/milkups/album/qa && cat > content/milkups/album/qa/RAW-EVIDENCE.md <<'RAWEOF'
(single heredoc write, exit 0)

--------------------------------------------------------------------
## COUNT
--------------------------------------------------------------------
Expected artifacts present on disk: 0 of 9.
(album-manifest.json, modules/, projects/, renders/, README.md, TRACKLIST.md, gtm/RELEASE-PLAN.md, sales/MONETIZATION.md, finance/BUDGET.md — all MISSING)
Directories created by this run for evidence only (not artifacts): 1 — content/milkups/album/qa/
